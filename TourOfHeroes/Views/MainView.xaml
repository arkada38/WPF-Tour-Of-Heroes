<Window
    x:Class="TourOfHeroes.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TourOfHeroes.Views"
    xmlns:properties="clr-namespace:TourOfHeroes.Properties"
    xmlns:converters="clr-namespace:TourOfHeroes.Converters"
    Title="{Binding Title}"
    Width="{Binding Source={x:Static properties:Settings.Default}, Path=Width, Mode=TwoWay}"
    Height="{Binding Source={x:Static properties:Settings.Default}, Path=Height, Mode=TwoWay}"
    Left="{Binding Source={x:Static properties:Settings.Default}, Path=Left, Mode=TwoWay}"
    Top="{Binding Source={x:Static properties:Settings.Default}, Path=Top, Mode=TwoWay}"
    WindowState="{Binding Source={x:Static properties:Settings.Default}, Path=WindowState, Mode=TwoWay}">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVis" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            HorizontalAlignment="Center"
            FontSize="48"
            Text="{Binding Title}" />

        <TextBlock
            Grid.Row="1"
            FontSize="24"
            Text="My Heroes" />
        <ListView
            Grid.Row="2"
            ItemsSource="{Binding Heroes}"
            SelectedItem="{Binding Hero, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Id}" Header="Id" />
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="3" Visibility="{Binding Hero, Converter={StaticResource NullToVis}}">
            <TextBlock FontSize="36" Text="{Binding Hero.Name, StringFormat=\{0\} details!}" />
            <TextBlock FontSize="24" Text="{Binding Hero.Id, StringFormat=Id: {0}}" />
            <TextBox Text="{Binding Hero.Name, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</Window>
